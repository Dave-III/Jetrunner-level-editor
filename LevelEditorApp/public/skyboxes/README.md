# JETRUNNER editor skybox previews

Place equirectangular (2:1) skybox images here and map each image to its
JETRUNNER Scenario ID in `src/visual-manifest.ts`.

These files are editor-only references. The exported level continues to store
the real Scenario and Environment names used by the game.
